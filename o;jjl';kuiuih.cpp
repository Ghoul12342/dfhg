#include <iostream>
using namespace std;
int main() {
	//If15.ƒаны три числа.Ќайти сумму двух наибольших из них
	
	/*float a, b, c;
	cin >> a >> b >> c;
	if (a > c&& b > c) {
		cout << a + b;
	}
	else if (c > a&& b > a) {
		cout << c + b;
	}
	else if (a > b&& c > b) {
		cout << c + a;
	}
	else if (c > a&& b > a) {
		cout << c + b;
	}*/
	//If16.ƒаны три переменные вещественного типа : A, B, C.≈сли их значени€
		//упор€дочены по возрастанию, то удвоить их; в противном случае заменить значение каждой переменной на противоположное.¬ывести новые
		//значени€ переменных A, B, C
	/*float a, b, c;
	cin >> a >> b >> c;
	if (c<b<a) {
		a = a * a;
	    b = b * b;
		c = c * c;
		cout <<a<<b<<c<<endl;
	}
	else {
		a = -a;
		b = -b;
		c = -c;
		cout << a << b << c<<endl;
	}*/
	








}